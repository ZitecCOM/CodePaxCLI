{
    "require": {
        "symfony/console": "^2.7"
    },

    "autoload": {
        "classmap": [
        	"src/AppBundle/Model/",
            "commands/",
        	"vendor/zitec/"
        ]
    }
}
